<%= form_for(@groupimage) do |f| %>
  <% if @groupimage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groupimage.errors.count, "error") %> prohibited this groupimage from being saved:</h2>

      <ul>
      <% @groupimage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.text_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
